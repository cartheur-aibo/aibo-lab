# hello_body

Minimal ERS-7 OPEN-R starter object.

This is intentionally a scaffold, not a guaranteed drop-in binary, because the exact includes, object descriptors, and build flags depend on the installed Sony OPEN-R SDK package and sample tree.

Start by copying the structure of a known-working Sony sample object, then replace its behavior with the logic in `HelloBody.cc`.
